% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_hm_orthologs.R
\name{convert_hm_orthologs}
\alias{convert_hm_orthologs}
\title{Convert Gene IDs Between Human and Mouse via Orthology}
\usage{
convert_hm_orthologs(
  IDs,
  from_species = c("human", "mouse"),
  to_species = c("mouse", "human"),
  from_type = c("symbol", "ensembl"),
  to_type = c("symbol", "ensembl", "both"),
  multiple = FALSE,
  high_confidence_only = TRUE,
  mirrors = ENSEMBL_MIRRORS,
  cache_dir = getOption("IDConverter.datapath", tempdir()),
  verbose = TRUE
)
}
\arguments{
\item{IDs}{a character vector of gene symbols or Ensembl IDs.}

\item{from_species}{source species: \code{"human"} (default) or \code{"mouse"}.}

\item{to_species}{target species: \code{"mouse"} (default) or \code{"human"}.
Must differ from \code{from_species}.}

\item{from_type}{type of input IDs: \code{"symbol"} (default) or \code{"ensembl"}.}

\item{to_type}{type of output IDs: \code{"symbol"} (default), \code{"ensembl"},
or \code{"both"} to return both columns.}

\item{multiple}{if \code{TRUE}, return a \code{data.frame} with all ortholog
matches (e.g., one-to-many relationships).}

\item{high_confidence_only}{if \code{TRUE} (default), restrict to orthologs
with high confidence (confidence == 1) in Ensembl.}

\item{mirrors}{character vector of Ensembl mirror URLs.}

\item{cache_dir}{directory to cache query results. Set \code{NULL} to skip.}

\item{verbose}{if \code{TRUE}, print progress messages.}
}
\value{
If \code{multiple = FALSE}, a character vector of converted IDs
(NA for unmatched). If \code{multiple = TRUE} or \code{to_type = "both"},
a \code{data.frame}.
}
\description{
Maps gene symbols or Ensembl IDs between human and mouse using
Ensembl BioMart orthology data. This resolves homologous gene
relationships (e.g., human TP53 <-> mouse Trp53).
}
\details{
\strong{Requirements:} This function requires the Bioconductor package
\pkg{biomaRt}. Install it with:\preformatted{
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("biomaRt")
}

Orthology data is queried live from Ensembl and cached locally.
}
\examples{
\donttest{
# Human symbol -> mouse symbol
convert_hm_orthologs(c("TP53", "KRAS", "EGFR"))

# Mouse symbol -> human symbol
convert_hm_orthologs(c("Trp53", "Kras"), from_species = "mouse", to_species = "human")

# Human Ensembl -> mouse Ensembl
convert_hm_orthologs("ENSG00000141510", from_type = "ensembl", to_type = "ensembl")
}
}
