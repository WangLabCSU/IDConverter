% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_annotables.R
\name{resolve_gene_aliases}
\alias{resolve_gene_aliases}
\title{Resolve Gene Symbol Aliases}
\usage{
resolve_gene_aliases(symbols, annotable, multiple = FALSE)
}
\arguments{
\item{symbols}{a character vector of gene symbols to resolve.}

\item{annotable}{a \code{data.frame} from \code{\link[=build_annotables]{build_annotables()}} with
\code{include_synonyms = TRUE}. Must contain \code{symbol} and \code{synonym} columns.}

\item{multiple}{if \code{TRUE}, return a \code{data.frame} with all matches
(including cases where one alias maps to multiple genes).}
}
\value{
a \code{data.frame} (tibble) with columns:
\item{query}{the input gene symbols}
\item{symbol}{resolved official gene symbol (\code{NA} if unmatched)}
\item{ensgene}{resolved Ensembl gene ID (\code{NA} if unmatched)}
When \code{multiple = TRUE}, each row is a single query→match pair,
so one query may appear in multiple rows.
}
\description{
Maps gene symbols through known aliases (synonyms) to their current
official symbols and Ensembl gene IDs. This helps when working with
outdated or alternative gene names (e.g., "MLL" → "KMT2A").
}
\examples{
\donttest{
# Build annotables with synonym support
ann <- build_annotables("grch38", include_synonyms = TRUE, tx2gene = FALSE)

# Resolve aliases
resolve_gene_aliases(c("TP53", "MLL", "NOTAGENE"), ann[[1]])

# Multiple match mode
resolve_gene_aliases(c("TP53", "MLL"), ann[[1]], multiple = TRUE)
}
}
