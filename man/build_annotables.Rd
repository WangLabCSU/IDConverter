% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_annotables.R
\name{build_annotables}
\alias{build_annotables}
\title{Build Gene Annotation Tables from Ensembl BioMart}
\usage{
build_annotables(
  recipes = NULL,
  tx2gene = TRUE,
  include_synonyms = FALSE,
  mirrors = ENSEMBL_MIRRORS,
  cache_dir = getOption("IDConverter.datapath", tempdir()),
  verbose = TRUE
)
}
\arguments{
\item{recipes}{character vector of recipe names to build, or \code{NULL}
to build all available recipes. Use \code{ls_annotables()} to see which
organisms are available (matching the "gene" + "tx2gene" table names).
Recipe names include \code{"grch38"}, \code{"grch37"}, \code{"grcm38"}, \code{"bdgp6"},
\code{"galgal5"}, \code{"rnor6"}, \code{"mmul801"}, \code{"wbcel235"}, \code{"cfamiliaris"},
\code{"drerio"}, \code{"sscrofa"}.}

\item{tx2gene}{if \code{TRUE} (default), also build transcript-to-gene
mapping tables (appended with \verb{_tx2gene} suffix).}

\item{include_synonyms}{if \code{TRUE}, also fetch \code{external_synonym} (gene
name aliases) from BioMart and include a \code{synonym} column in the
output tables. Default \code{FALSE} for faster queries.}

\item{mirrors}{character vector of Ensembl mirror URLs to try in order.
The function automatically falls back to the next mirror on failure.}

\item{cache_dir}{directory path to cache downloaded tables as \code{.rda}
files. Set to \code{NULL} to skip caching. Default uses the package's
data path (customizable via \code{options(IDConverter.datapath = ...)}).}

\item{verbose}{if \code{TRUE}, print progress messages.}
}
\value{
a named list of \code{data.frame} objects (tibbles). The list names
correspond to recipe names (and \verb{_tx2gene} variants if \code{tx2gene = TRUE}).
Returns \code{invisible(NULL)} on total failure.
}
\description{
Queries Ensembl BioMart directly to build up-to-date gene annotation
tables ("annotables") using recipes derived from the
\href{https://github.com/stephenturner/annotables}{annotables} package.
This provides the latest annotations from Ensembl when internet is
available, as an alternative to the fixed-version tables stored on Zenodo.
}
\details{
\strong{Requirements:} This function requires the Bioconductor package
\pkg{biomaRt}. Install it with:\preformatted{
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager")
BiocManager::install("biomaRt")
}

\strong{Mirror fallback:} Ensembl mirrors can be unreliable. The function
tries each mirror in \code{mirrors} for each recipe until one succeeds.
If all mirrors fail for a recipe, that recipe is skipped with a warning.

\strong{Caching:} When \code{cache_dir} is set, successfully downloaded tables
are saved as \code{.rda} files. Subsequent calls with the same \code{cache_dir}
will load from cache instead of re-querying, unless \code{cache_dir = NULL}.
}
\examples{
\donttest{
# Build a single annotable table
grch38 <- build_annotables("grch38", tx2gene = FALSE)
head(grch38[[1]])

# Build all available tables (requires internet + biomaRt)
all_tables <- build_annotables()
names(all_tables)
}
}
\references{
\itemize{
\item \url{https://github.com/stephenturner/annotables}
\item \url{https://useast.ensembl.org/info/data/biomart.html}
}
}
\seealso{
\code{\link[=load_data]{load_data()}} for loading fixed-version tables from Zenodo,
\code{\link[=ls_annotables]{ls_annotables()}} for listing available tables.
}
